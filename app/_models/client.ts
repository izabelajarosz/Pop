export class Client {
    id: number;
    firstName: string;
    lastName: string;
    // street: string;
    // streetNumber: string;
    // homeNumber: string;
    // city: string;
    // postCode: string;
     pesel: string;
       birthDate: string;
       policyNumber: number;
}